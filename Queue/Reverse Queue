

public class Solution {

	public static void reverseQueue(Queue<Integer> q) {
		// Write your code here  
        if(q.isEmpty()){
            return;
        }
        int temp=q.dequeue();
        reverseQueue(q);
        q.enqueue(temp);
    

	}
}
